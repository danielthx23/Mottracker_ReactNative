import { View, Text } from "react-native";

const UsuarioScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Motoca Screen</Text>
        </View>
    );
}

export default UsuarioScreen;